export const SidebarFeed = ({ items, children }) => {
  const stubbedItems = [
    {
      title: "How I got 20k Leads On Autopilot",
      videoSrc: "/videos/OpenSwarm.mp4",
      description: "OpenSwarm · 1.8M views · 11 months ago",
    },
    {
      title: "Building a scraper that never gets blocked",
      videoSrc: "/videos/OpenSwarm.mp4",
      description: "OpenSwarm · 3.6M views · 10 days ago",
    },
    {
      title: "Why we automate every outbound message",
      videoSrc: "/videos/OpenSwarm.mp4",
      description: "OpenSwarm · 1.3M views · 1 year ago",
    },
    {
      title: "The full workflow, start to finish",
      videoSrc: "/videos/OpenSwarm.mp4",
      description: "OpenSwarm · 2.6M views · 3 days ago",
    },
    {
      title: "Enriching leads before they hit your inbox",
      videoSrc: "/videos/OpenSwarm.mp4",
      description: "OpenSwarm · 12K views · 5 hours ago",
    },
  ];

  const feed = items ?? stubbedItems;

  const feedStyle = {
    display: "flex",
    flexDirection: "column",
    gap: "1.25rem",
    maxWidth: "402px",
    padding: "0 0.5rem",
    margin: 0,
  };

  const itemStyle = {
    display: "flex",
    gap: "0.75rem",
    borderRadius: "0.75rem",
    cursor: "pointer",
    textDecoration: "none",
  };

  const thumbWrapStyle = {
    position: "relative",
    flex: "0 0 168px",
    width: "168px",
    aspectRatio: "16 / 9",
    borderRadius: "0.5rem",
    overflow: "hidden",
    background: "#0f0f0f",
  };

  const thumbStyle = {
    width: "100%",
    height: "100%",
    objectFit: "cover",
    display: "block",
    margin: 0,
  };

  const metaStyle = {
    display: "flex",
    flexDirection: "column",
    gap: "0.15rem",
    minWidth: 0,
  };

  const titleStyle = {
    margin: 0,
    fontSize: "0.875rem",
    fontWeight: 500,
    lineHeight: "1.25rem",
    display: "-webkit-box",
    WebkitLineClamp: 2,
    WebkitBoxOrient: "vertical",
    overflow: "hidden",
  };

  const descStyle = {
    margin: 0,
    fontSize: "0.75rem",
    lineHeight: "1.1rem",
    opacity: 0.6,
    display: "-webkit-box",
    WebkitLineClamp: 2,
    WebkitBoxOrient: "vertical",
    overflow: "hidden",
  };

  const transparentHeaderStyle = {
    color: "transparent",
    width: 0,
  };

  const subHeaderStyle = {
    margin: 0,
    marginBottom: "0.75rem",
    opacity: 0.75,
    fontWeight: 500,
  };

  const relatedVidHeaderContainerStyle = {
    display: "flex",
    flexDirection: "row",
    alignItems: "flex-end",
    justifyContent: "flex-start",
    height: "fit-content",
  };

  const feedOutterContainerStyle = {
    display: "flex",
    flexDirection: "column",
    alignItems: "flex-start",
    justifyContent: "flex-start",
    height: "fit-content",
    width: "fit-content",
  };

  return (
    <div className="prose dark:prose-invert max-w-none" style={feedOutterContainerStyle}>
      <div style={relatedVidHeaderContainerStyle}>
        <h3 style={subHeaderStyle}> Related videos </h3>
        <h1 style={transparentHeaderStyle}>.</h1>
      </div>
      <div className="not-prose" style={feedStyle}>
        {children}
        {feed.map((item, index) => (
          <div key={index} style={itemStyle}>
            <div style={thumbWrapStyle}>
              <video
                src={item.videoSrc}
                muted
                playsInline
                preload="metadata"
                style={thumbStyle}
              />
            </div>
            <div style={metaStyle}>
              <p style={titleStyle}>{item.title}</p>
              {item.description && <p style={descStyle}>{item.description}</p>}
              {item.children}
            </div>
          </div>
        ))}
      </div>
    </div>
  );
};



export const FeedHeader = ({ 
  title, 
  videoSrc, 
  description, 
  children 
}) => {

  const feedContainerStyle = {
    borderRadius: "1rem",
    maxWidth: "860px",
  };

  const videoStyle = {
    width: "100%",
    height: "100%",
    objectFit: "cover",
    borderRadius: "1rem",
  };

  return (
    <div className="prose dark:prose-invert" style={feedContainerStyle}>
      <h1>{title}</h1>
      <video src={videoSrc} controls style={videoStyle} />
      <p>{description}</p>
      <div className="prose dark:prose-invert max-w-none">
        {children}
      </div>
    </div>
  );
};
